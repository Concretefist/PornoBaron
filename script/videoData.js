const videoData = [{
    id: 1,
    videoDescription: 'Fat bitch getting railed',
}
]

// über dataset könnte ich jedem video beim anklicken eine ID zuordnen oder so und diese ID wird dann in einem neuen fenster geöffnet